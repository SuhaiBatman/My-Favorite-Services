/**
 * Fallback before `supabaseEnv.ts` is generated (`npm install` / Metro via app.config).
 */
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
export const SUPABASE_USE_LOCAL = '';
