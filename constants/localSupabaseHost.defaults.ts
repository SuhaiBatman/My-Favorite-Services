/**
 * Default local Supabase host (simulator / before LAN sync).
 * Copied to `localSupabaseHost.ts` on install; that file is gitignored and updated by `npm run lan:sync`.
 */
export const LOCAL_SUPABASE_HOST = '127.0.0.1';
export const LOCAL_SUPABASE_API_URL = 'http://127.0.0.1:54321';
