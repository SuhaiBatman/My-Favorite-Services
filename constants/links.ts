/** App Store / Play Store URL — set when ready; empty skips store redirect */
export const APP_DOWNLOAD_URL = '';

/** Custom URL scheme (must match app.json `scheme`) */
export const APP_SCHEME = 'myfavoriteservices';
